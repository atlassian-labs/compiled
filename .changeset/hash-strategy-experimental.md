---
'@compiled/babel-plugin': minor
'@compiled/css': patch
'@compiled/react': minor
---

add experimental `hashStrategy` option for `cssMap`, only used for internal use.
make `ax` supports longer group hash.

```

```
