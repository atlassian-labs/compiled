---
'@compiled/babel-plugin': patch
---

Support TypeScript `satisfies` expressions during static CSS evaluation, including `cssMap` values, nested values, and member access through satisfies-wrapped bindings.
