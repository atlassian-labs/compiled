import noEmotionCSS from './rules/no-emotion-css';

export const rules = {
  'no-emotion-css': noEmotionCSS,
};

export const configs = {
  plugins: ['@compiled'],
  recommended: {
    rules: {},
  },
};
