{
  "name": "@private/benchmarks",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "all": "yarn ax && yarn ssr && yarn stylesheet",
    "ax": "NODE_ENV=production && ts-node ./src/ax/benchmark",
    "ssr": "NODE_ENV=production && ts-node ./src/ssr/benchmark",
    "stylesheet": "NODE_ENV=production && ts-node ./src/stylesheet/benchmark"
  },
  "devDependencies": {
    "@compiled/react": "*",
    "benchmark": "^2.1.4",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "ts-node": "^9.1.1"
  }
}
